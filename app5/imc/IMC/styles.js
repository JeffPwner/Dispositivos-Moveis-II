import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  campoNum: {
    textAlign: 'center',
    margin: 10,
    fontSize: 20,
    border: '2px solid blue',
    padding: 15,
  },
  classificar: {
    textAlign: 'left',
    fontSize: 25,
    color: 'red',
  },

});

export { styles };
